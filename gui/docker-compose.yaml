services:
  terraria-gui:
    build: .
    container_name: terraria-gui
    ports:
      - "3000:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      GUI_PASSWORD: changeme  # Change this before exposing to a network
    restart: unless-stopped
